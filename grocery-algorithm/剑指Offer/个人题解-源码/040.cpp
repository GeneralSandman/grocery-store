#include <iostream>
#include <map>
#include <set>
#include <unordered_map>
#include <vector>

using namespace std;

class Solution {
public:
    int maximalRectangle(vector<string>& matrix) {
        vector<int> leftIndex(matrix.size(), 0);
        vector<int> rightIndex(matrix.size(), matrix.size() - 1);
    }

    void updateLeftIndex(vector < stringvector<int> & leftIndex)
};

int main() {
    Solution solution;

    return 0;
}
